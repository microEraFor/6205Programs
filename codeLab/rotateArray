 public void rotate(int[] nums, int k) {
        int[] t=new int[nums.length];
        for(int i=0;i<nums.length;i++){
            t[(i+k)%(nums.length)]=nums[i];
        }
        for(int j=0;j<t.length;j++) nums[j]=t[j];
    }
